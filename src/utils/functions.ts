import { compareAsc, compareDesc } from "date-fns"
import { TodoModel } from "./models"

// export function sort(filterValue: string, todos: TodoModel[]) {
//   let dates: Date[] = []

//   todos.forEach(todo => dates.push(new Date(todo.dueDate)))
//   const sorted = dates.sort(compareAsc)
//   console.log(sorted)
// }
